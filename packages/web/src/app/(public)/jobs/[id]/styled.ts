'use client';

import { blue } from '@/utils/color';
import { Box, styled } from '@mui/system';

export const Title = styled('h2')`
  font-size: 28px;
  font-weight: 700;
  line-height: 1.5;
  margin-bottom: 18px;
  color: ${blue[500]};
`;
